const divide = (a, b) => {
    if (b === 0) {
      return "can't divide by zero";
    }
    return a / b;
  };
  
 
  const square = (x) => {
    return x * x;
  };
  

  const add = (a, b) => {
    return a + b;
  };